/** @type {import('next').NextConfig} */
const nextConfig = {
    // add host image
    images: {
        domains: [
            "tailwindui.com",
            "res.cloudinary.com",
            "i.ibb.co"
        ]
    },
}

module.exports = nextConfig
